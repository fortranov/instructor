 # 1. Обновить код
  cd ~/www/instructor
  git pull

  # 2. Перезапустить backend
  docker-compose restart backend

  # 3. Скопировать скрипт в контейнер
  docker cp backend/fix_strength_complete.py backend:/app/fix_strength_complete.py

  # 4. Запустить скрипт
  docker exec -it backend python /app/fix_strength_complete.py

  Скрипт автоматически:
  - ✅ Установит has_strength_training=1 для всех пользователей с триатлоном
  - ✅ Добавит по 1 силовой тренировке в неделю для фаз base/build/peak
  - ✅ Если день свободен - добавит туда, если нет - в день с минимальной нагрузкой

  После запуска:
  1. Обновите страницу в браузере (Ctrl+F5)
  2. Силовые тренировки должны появиться с иконкой 🏋️

  Покажите вывод скрипта, если что-то пойдет не так!